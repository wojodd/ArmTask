# عناوين الصفحات مع محتوياتها 
### armtask 
1. في هذه الصفحة قمت بعمل فورم يحتوي على 6 سلايدرز  
2. زر حفظ عند الضغط عليه سيتم حفظ قيم السلايدرز التي حددها المستخدم في قاعدة البيانات
3. زر التشغيل اذا ضغط عليه المستخدم سيتم وضع قيمة 1 في قاعدة البيانات , اما في حال عدم الضغط على زر التشغيل فستوضع قيمة 0 تلقائيا في قاعدة البيانات 
### insertDB
1.  في هذه الصفحة قمت بالربط مع قاعدة البيانات وادخال قيم السلايدرز المختارة في قاعدة البيانات 
2.  ربط زر التشغيل مع قاعدة البيانات وسيكون تلقائيا موضوع على 0 وعندما يتم الضغط على زر التشغيل في الواجهة سيتم تحديث القيمة ب1
### selectLast
1. عرض اخر صف تم حفظه في قاعدة البيانات "قيمة كل سلايدرز مع قيمة عمود التشغيل"ء
### style22
1. للتنسيق
